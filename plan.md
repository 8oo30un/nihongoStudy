# 일본어 학습 웹앱 기획 (nihongo-study)

개인용 일본어 학습 + **워홀 현장 회화 수첩**. 맥북에 일본어 IME를 켜지 않아도 가상 패드로 가나를 치고, 하루 문장·복습·일기·음성을 돌린다. 폰에서는 홈 화면 앱처럼 열고, 돋보기에 `사랑` 또는 `すき`를 치면 관련 문장이 바로 나온다.

이 문서는 구현 전 합의서다. 아래 **확정된 결정**을 기준으로 만든다.

---

## 0. 확정된 결정

| # | 질문 | 결정 |
| --- | --- | --- |
| 1 | 문자 | **가나로 쓰고 공부**한다. 한자는 입력의 기본이 아니다. 다만 문장마다 **한자 보기**로 “이게 어떤 한자인지”를 나중에 확인할 수 있다. |
| 2 | 저장 | 브라우저 IndexedDB가 아니라 **Next.js 안 SQLite**. 서버 라우트에서 읽고 쓴다. |
| 3 | 첨삭 | **유료 API 없음.** 무료 로컬 엔진만. 본선은 Ollama, 보조는 형태소 분석. |
| 4 | 음성 | **piper-plus 우선** (MIT, 무료, 브라우저에서 일본어 TTS). 모델 로딩 전에는 Web Speech를 폴백. |
| 5 | UI | **종이 노트 감성.** 크림지, 줄노트, 잉크, 도장 같은 카테고리. |
| 6 | 형태 | **웹앱 + 앱 뷰(PWA).** 아이폰/안드로이드 홈 화면에 두고 단독 창으로 연다. |
| 7 | 현장 사용 | 워홀에서 카테고리·**전역 검색**으로 바로 꺼내 쓴다. 한글·가나 모두 검색. |

---

## 1. 한 줄 요약

| 구분 | 내용 |
| --- | --- |
| 사용자 | 나 혼자. 맥에서 공부하고, 워홀 현장에선 폰 앱 뷰로 검색 |
| 입력 | 가상 가나 패드 + (맥) 로마자→가나. 한자 직접 입력은 안 함 |
| 학습 | 하루 약 5문장 (가나 JP + 한글 해석), 오답은 다음날 복습, 일기 |
| 현장 | 카테고리 + 돋보기 검색 (`사랑` / `すき` / `あい` → 관련 문장) |
| 스택 | Next.js + SQLite + PWA + piper-plus + Ollama(첨삭) |

---

## 2. 반드시 넣을 기능

### 2.1 가상 일본어 키보드 패드

- 일본어 입력칸 포커스 시 화면 하단에 **50음 패드**.
- 맥 OS 일본어 IME **없이** 가나 입력.
- 키를 누르면 **눌림 표시** (즉시 하이라이트 + 살짝 눌리는 모션).
- 물리 키를 눌러도 같은 패드 키가 빛나면 좋다.
- 레이어: 히라가나 / 가타카나, ゛゜, ゃゅょっ, ー, 。、？！, 공백, 삭제, 숫자·기호 2차.
- 한글 해석 칸에서는 패드를 닫거나 한글 입력을 막지 않는다.

### 2.2 가나 문장 + 한자 힌트

- 저장의 정본은 **가나 문장** (`jpKana`) + **한글 해석** (`koText`).
- 문장 카드에 **한자 보기** 토글.
  - 꺼짐: `きょうは かいしゃに いきます。`
  - 켜짐: `今日は 会社に 行きます。` 위에 가나 후리가나 (ruby).
- 한자열은 입력할 때 필수가 아니다. 나중에 변환해서 `jpKanji`에 캐시한다.
- 가나만 있는 일본어는 띄어쓰기가 없으면 한자 변환이 흔들린다. **학습용으로 어절 사이 공백을 허용**한다 (예: `きのう かいしゃに いきました`).

### 2.3 하루 문장 (약 5개)

- 날짜별로 일본어(가나) + 한글 해석 + 카테고리.
- 기본 5개, 설정에서 변경 가능.
- 내가 **맞음 / 틀림**을 직접 체크.
- 틀린 문장은 **다음날 복습 큐**.

### 2.4 카테고리 (공부 + 워홀 현장)

문장 저장 시 하나(또는 나중에 여러 개) 고른다. 기본 시드:

**생활**
- 일상, 사교, 연애

**이동·공간**
- 교통, 공항·입국, 숙소, 쇼핑, 음식점

**일·행정**
- 회사, 아르바이트, 관공서·비자, 은행·세금

**응급**
- 병원·약국, 긴급

커스텀 카테고리 추가 가능. 워홀 현장에서는 카테고리 그리드가 **큰 버튼**이라 장갑/한 손에도 눌리게.

### 2.5 전역 검색 (워홀의 핵심)

헤더/하단 탭의 **돋보기**. 한 칸에 한글이든 가나든 넣는다.

예:

| 입력 | 나와야 하는 것 |
| --- | --- |
| `사랑` | 해석에 “사랑”이 들어간 문장, 연애 카테고리 문장 |
| `すき` / `すきた` | 가나 본문에 그 조각이 있는 문장 |
| `숙소` | 숙소 카테고리 + 해석에 “숙소/호텔/집”이 있는 문장 |
| `びょういん` | 병원 관련 가나 문장 |

동작:

- 입력 중 디바운스(150~250ms) 후 결과.
- 매칭 필드: `koText`, `jpKana`, `jpKanji`, 카테고리 이름, **검색 별칭** (`keywords`, 예: 사랑, 좋아, すき, あい).
- 결과 카드: 가나 문장, 한글, 카테고리, 재생 버튼. 탭하면 상세(한자 보기 포함).
- 결과가 없으면 “이 표현을 오늘 문장으로 추가” 빠른 진입.
- 한글 1~2글자(`일`, `집`)도 되게. CJK는 공백 분리가 없어서 **부분 문자열 검색**이 기본이다.

### 2.6 일기

- 하루 1편. 본문은 가나 일본어, 한글 메모는 선택.
- 같은 날짜의 문장과 타임라인을 공유.
- **첨삭**: 무료 로컬 엔진 (Ollama). 원문은 보존하고 교정문은 따로 저장.

### 2.7 음성 읽기

- 문장·검색 결과·일기·입력칸에 재생.
- 일본어만 읽는다 (가나 또는 한자열 모두 TTS 가능).
- 엔진: **piper-plus** (무료 MIT). 첫 모델 다운로드 전/실패 시 Web Speech `ja-JP`.

### 2.8 웹앱 + 앱 뷰 (PWA)

- `display: standalone`, 아이콘, 스플래시, 테마색(크림지).
- iOS: “홈 화면에 추가”로 사파리 크롬 없이 연다.
- 앱처럼 보이게:
  - 하단 탭: `오늘` / `찾기` / `카테고리` / `일기` / `더보기`
  - 상단 주소창 없음 (standalone)
  - `100dvh` + safe-area (노치, 홈 인디케이터)
  - 터치 타깃 최소 44px
  - 스크롤 체이닝, 오버스크롤 바운스만 노트 느낌으로
- 가상 패드는 모바일에서 **바텀시트**. 탭 바와 겹치지 않게.

폰에서 DB를 쓰려면 Next 서버에 닿아야 한다. 로컬 맥만 켜 두면 같은 와이파이/테일스케일로 접속. 워홀 데이터는 배포(또는 항상 켜 둔 작은 서버)가 사실상 필요하다. 아래 3.2.

---

## 3. 기술적으로 걸리는 지점

### 3.1 가상 키보드 · 한자 힌트

입력은 가나만. 한자는 **조회용**.

| 단계 | 방식 | 판단 |
| --- | --- | --- |
| MVP | 50음 패드 + 눌림 표시 | 확정 |
| MVP+ | WanaKana 로마자→가나 (MIT, 무료) | 맥 타이핑용 |
| 한자 보기 | ① Mozc wasm (hechima, 무료)로 가나→한자 후보 ② 없으면 Ollama에 “가나 문장의 보통 한자 표기” JSON | 둘 다 유료 아님 |
| 표시 | ruby: 한자 위에 가나 | kuroshiro는 한자→가나용. 우리는 반대 방향이라 **변환 결과 + 원문 가나를 짝지어 ruby** |

가나만 이어 쓰면 `きょうはいしゃ`가 회사인지 의사인지 모호하다. 공백 권장 + 한자 보기에서 후보가 여러 개면 고르게.

### 3.2 Next.js 안의 DB — 난이도 중 (PWA와 충돌하는 지점)

요청은 “Next 안 DB”. 맞는 형태는 **프로젝트 파일 SQLite**다. Next.js 자체에 DB가 내장되어 있지는 않다.

| 선택 | 내용 |
| --- | --- |
| ORM | Drizzle |
| 드라이버 | `better-sqlite3` (로컬 맥 `npm run dev` / `node` 서버) |
| 파일 | `data/nihongo.db` (git 무시) |
| 검색 | SQLite `LIKE %질의%`로 시작. 문장이 많아지면 FTS5 `tokenize='trigram'` (한글·가나 부분일치). 1~2글자는 LIKE 폴백 |

**배포 함정**

- Vercel 서버리스는 파일 SQLite가 거의 불가능 (읽기전용, 인스턴스마다 디스크가 다름).
- 워홀 폰에서 쓰려면:
  1. 맥/미니PC에서 Next를 켜 두고 테일스케일/클라우드플레어 터널로 접속하거나
  2. 작은 VPS에 Next + SQLite를 올리거나
  3. 나중에 Turso(libSQL, 무료 티어)로 같은 Drizzle 스키마를 옮긴다.

앱 뷰(PWA)는 **UI가 앱처럼 보이는 것**이지, 서버 없이 폰에 DB가 생기는 것이 아니다. 오프라인은 나중에 읽기 캐시(최근 검색·즐겨찾기 카테고리)만 서비스워커로 넣는 편이 맞다. 쓰기는 온라인이 기본.

로그인/회원가입은 안 한다. 혼자 쓰므로 배포 시에는 간단한 앱 비밀번호 또는 테일스케일 뒤에 숨긴다.

### 3.3 검색 (한글 + 가나)

CJK는 단어 공백이 없다.

1. MVP: `jp_kana` / `ko_text` / `keywords` / `category.name` 에 `LIKE '%' || q || '%'`
2. 정규화: 히라·가타 통일해서 찾기 (`すき`와 `スキ`), 한글은 그대로, 로마자는 WanaKana로 가나화 후 한 번 더 검색 (`suki` → `すき`)
3. 키워드 필드: 문장 저장 시 내가 `사랑, すき, 好き`처럼 별칭을 넣거나, 나중에 자동 추출
4. 카테고리 이름도 매칭 (`연애` 입력 시 그 칸 전체)

워홀 UX: `/search`가 탭 한 번에 열리고, 키보드가 올라오면 결과만 스크롤.

### 3.4 첨삭 — 무료만

유료 OpenAI 등은 쓰지 않는다.

| 엔진 | 비용 | 역할 |
| --- | --- | --- |
| **Ollama + 로컬 모델** (Qwen2.5 등) | 무료, 맥 GPU/CPU | 학습자 첨삭 본선. 교정문 + 한글 설명 JSON |
| kuromoji / Lindera-wasm | 무료 | 형태소·읽기. 첨삭이 아님 |
| LanguageTool 셀프호스트 | 무료, JVM 무거움 | 맞춤법 보조. 초기엔 생략 가능 |
| piper-plus / Web Speech | 무료 | 음성. 첨삭 아님 |

Ollama가 꺼져 있으면 일기는 저장만 되고, 첨삭 버튼은 “로컬 엔진이 없습니다. Ollama를 켜 주세요.” 폰(워홀)에서는 첨삭보다 **검색·카테고리**가 우선이라, 첨삭은 맥 공부 세션 기능으로 둬도 된다.

### 3.5 음성 — piper-plus 우선

piper-plus는 **MIT, 서버/API 키 없음, 브라우저에서 합성**. 원하는 “무료”와 맞다.

- 첫 실행: HuggingFace에서 일본어 모델 다운로드 (수십 MB), IndexedDB에 캐시.
- 추상화: `speakJapanese(text)` → piper, 실패 시 `speechSynthesis` `ja-JP`.
- 클릭 제스처 후에만 재생 (브라우저 자동재생 차단).
- PWA 아이폰: WebGPU/WASM 제약이 있을 수 있음. 그때는 Web Speech 폴백이 필수.

### 3.6 종이 노트 UI

톤을 처음부터 토큰으로 고정한다. 다크 네온/기본 shadcn 그레이는 쓰지 않는다.

| 토큰 | 방향 |
| --- | --- |
| 배경 | 따뜻한 크림 (`#f4ecd8` 계열), 아주 약한 종이 결(노이즈) |
| 본문 영역 | 흰 노트 카드, 연한 가로줄 (일기·문장 입력) |
| 글자 | 한글 본문: 고운바탕/본명조 계열. 일본어: 시뽀리 명조 또는 젠 마루고. 너무 얇지 않게 |
| 잉크 | 남색~먹색 (`#2c3a4a`), 강조는 인주 레드/도장색 |
| 카테고리 | 종이 테이프·도장 칩 |
| 패드 | 기계식보다 **연필로 누른 칸**. 눌림은 연한 흑연 그림자 |
| 앱 뷰 | 하단 탭도 노트 인덱스 탭처럼 (오늘 / 찾기 / …) |

라이트 모드가 기본. 밤 공부가 필요하면 나중에 “스탠드 조명” 정도의 세피아 다크만 추가.

### 3.7 기술 스택 (확정안)

| 영역 | 선택 |
| --- | --- |
| 앱 | Next.js App Router + TypeScript |
| 스타일 | Tailwind + 위 노트 토큰 (CSS variables) |
| DB | Drizzle + better-sqlite3, `data/nihongo.db` |
| API | Route Handlers 또는 서버 액션. 검색은 GET `/api/search?q=` |
| PWA | `@ducanh2912/next-pwa` 또는 Serwist. `manifest.webmanifest` |
| 가나 | WanaKana |
| TTS | piper-plus, Web Speech 폴백 |
| 첨삭 | Ollama HTTP `localhost:11434` |
| 한자 보기 | hechima 또는 Ollama (Phase 6) |
| 날짜 | `Asia/Seoul` 또는 설정 타임존. 워홀이면 나중에 `Australia/Sydney` 등 변경 |

---

## 4. 화면 구조

앱 뷰 하단 탭이 기준이다. 맥 와이드에서는 왼쪽 노트 링 네비 + 같은 페이지.

```
/                  오늘 — 문장 5칸, 복습 N, 일기 점
/search            찾기 — 돋보기 큰 칸, 최근 검색, 즉시 결과
/categories        카테고리 그리드 (워홀용 큰 타일)
/categories/[slug] 그 칸 문장 목록 + 그 안에서 재검색
/review            오늘 복습
/diary             달력
/diary/[date]      작성·첨삭·TTS·한자 보기
/sentences/new     오늘 문장 입력 (탭 없이도 / 에서 가능)
/settings          일일 개수, TTS, 패드, DB 백업, PWA 안내
```

가상 키보드는 일본어 필드 포커스 시 전역 바텀시트.

---

## 5. 데이터 초안

```
Category
  id, name, slug, emoji, sortOrder, createdAt

Sentence
  id
  jpKana             // 정본
  jpKanji            // nullable, 한자 보기 캐시
  koText
  keywords           // "사랑,すき,好き" 콤마/공백 구분
  categoryId
  createdOn          // YYYY-MM-DD
  selfMark           // unset | ok | wrong
  dueOn
  lastReviewedOn
  reviewCount

DiaryEntry
  id
  date               // unique
  jpKana
  jpKanji            // nullable
  koNote
  correctionJson
  createdAt, updatedAt

SearchQuery (optional)
  q, searchedAt      // 최근 검색어

Setting
  dailySentenceGoal  // 5
  ttsEngine          // piper | web-speech
  timezone
```

백업: 설정에서 SQLite 파일 또는 JSON 덤프 다운로드.

검색 SQL 스케치:

```sql
SELECT s.* FROM sentence s
JOIN category c ON c.id = s.category_id
WHERE s.ko_text LIKE '%' || :q || '%'
   OR s.jp_kana LIKE '%' || :q || '%'
   OR IFNULL(s.jp_kanji,'') LIKE '%' || :q || '%'
   OR IFNULL(s.keywords,'') LIKE '%' || :q || '%'
   OR c.name LIKE '%' || :q || '%'
ORDER BY s.created_on DESC
LIMIT 50;
```

질의는 서버에서 히라가나화·공백 트림 후 바인딩. `%` `_` 는 escape.

---

## 6. 순차 로드맵

### Phase 0 — 기획

- [x] 결정 반영한 이 문서
- [ ] Phase 1 구현 시작 (다음 액션)

### Phase 1 — 뼈대 + 노트 UI + 패드 + PWA 껍데기

- Next.js 스캐폴드, 크림지 토큰, 본명조 계열 폰트
- 하단 탭 레이아웃 (standalone에서도 동일)
- `manifest` + 아이콘, `apple-mobile-web-app-capable`
- `JapanesePad` 50음, 눌림 표시, 포커스된 일본어 칸에 insert
- 데모 textarea로 패드만 검증

완료: 맥에서 패드만으로 `こんにちは。` 입력, 키 눌림이 보이고, 폰 폭에서 탭바가 노트처럼 보인다.

### Phase 2 — SQLite + 문장 + 카테고리

- Drizzle 스키마, `data/nihongo.db`
- 워홀 포함 카테고리 시드
- 오늘 문장 5칸 (가나 + 한글 + 카테고리 + keywords)
- 카테고리 그리드 / 목록

완료: 문장이 DB에 남고, 카테고리에서 다시 열린다. 새로고침 후에도 유지.

### Phase 3 — 찾기 (돋보기)

- 전역 `/search`, 헤더·탭에서 진입
- 한글·가나·로마자(가나화)·키워드·카테고리명 부분일치
- 결과에서 TTS 재생, 해당 카테고리로 점프
- 빈 결과 → 문장 추가로

완료: `사랑`과 `すき` 둘 다 같은 연애 문장이 뜬다.

### Phase 4 — 오답 + 다음날 복습

- 맞음/틀림, `dueOn`, `/review`, 해석 가리기

완료: 어제 틀린 문장이 오늘 복습에 나온다.

### Phase 5 — 일기 + piper-plus

- 날짜별 일기, 달력 점
- `speakJapanese`: piper-plus 우선, Web Speech 폴백
- 문장/검색/일기 재생 버튼

완료: 노트에서 일본어가 들린다. 아이폰 PWA에서는 폴백이 되더라도 재생은 된다.

### Phase 6 — 한자 보기 + 무료 첨삭

- 문장/일기 **한자 보기** (Mozc wasm 또는 Ollama)
- ruby 표시, `jpKanji` 캐시
- 일기 첨삭 → Ollama, 원문 보존
- (여유) 후리가나 토글은 한자 보기와 동일 UI

완료: 가나로 쓴 문장에서 한자를 확인하고, 맥에서 일기 첨삭을 받을 수 있다. Ollama가 없으면 작성·한자 보기의 다른 경로는 산다.

### Phase 7 — 현장·학습 강화

- 즐겨찾기 / “오늘 쓸 문장” 핀
- FSRS, 스트릭, JSON/SQLite 백업
- 오프라인: 최근 검색·핀 문장 캐시
- 배포(VPS 또는 Turso) + 간단한 접근 제한
- ts-fsrs, 클로즈(조사)

---

## 7. UX 디테일

### 패드

- `pointerdown`에서 즉시 눌림. 본문은 패드 높이만큼 `padding-bottom`.
- 모바일: 탭바와 패드가 동시에 안 겹치게. 패드가 열리면 탭바를 가리거나 패드를 탭 위에 둔다.
- Esc / “내리기”로 접기.

### 찾기 (워홀)

- 페이지 상단 고정 돋보기, placeholder 예: `사랑, すき, 숙소…`
- 최근 검색 칩
- 결과는 한 손 엄지로 재생 가능하게 오른쪽 스피커 버튼

### 복습

- 기본: 가나 공개, 한글은 탭. 한자 보기는 복습 중 선택.
- “아직 어렵다”만 다시 due.

### 일기

- 줄노트 textarea.
- 첨삭은 원문 옆 또는 아래 별도 잉크색. 원문 덮어쓰기 없음.

### 앱 뷰

- 설치 안내를 설정에 한 줄: iOS 공유 → 홈 화면에 추가.
- standalone일 때 헤더 뒤로가기는 직접 구현 (사파리 제스처만 믿지 않기).

---

## 8. 리스크

| 리스크 | 대응 |
| --- | --- |
| 가나만 이어 쓰면 한자 변환이 틀림 | 어절 공백 권장, 후보 여러 개면 선택 |
| SQLite + Vercel | 로컬/VPS/Turso. 서버리스에 파일 DB 올리지 않음 |
| PWA인데 서버 꺼지면 폰에서 검색 불가 | 워홀 전에 배포 또는 상시 서버. 핀 문장 오프라인은 Phase 7 |
| 아이폰에서 piper WASM 실패 | Web Speech 폴백 |
| Ollama를 폰에서 기대 | 첨삭은 맥 기능으로 안내 |
| 검색이 너무 광역 (`일`) | 짧은 글자는 카테고리 가중, 결과 상한 |

---

## 9. 참고 링크

- WanaKana: https://github.com/WaniKani/WanaKana/
- hechima (Mozc wasm): https://github.com/msonrm/hechima
- piper-plus: https://www.npmjs.com/package/piper-plus
- Ollama: https://ollama.com
- Drizzle + SQLite: https://orm.drizzle.team/docs/get-started-sqlite
- ts-fsrs: https://github.com/open-spaced-repetition/ts-fsrs
- kuroshiro: https://github.com/hexenq/kuroshiro

---

## 10. 다음 액션

결정은 닫혔다. 다음 작업은 **Phase 1**: Next.js 스캐폴드, 종이 노트 톤, 하단 탭(앱 뷰), 가상 가나 패드.

구현을 시작하라고 하면 Phase 1부터 코드로 들어가면 된다.
